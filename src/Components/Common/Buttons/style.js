import { styled } from "@mui/material/styles";
import { Button } from "@mui/material";

export const StyledButtonWrapper = styled(Button)(({ variant }) => ({
//   display:'flex',
//   backgroundColor: variant === "contained" ? "green" : "black",
  
}));
